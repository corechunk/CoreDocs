# Method: read()
## Class: `java.io.InputStream`

Reads the next byte of data from the input stream.

### Signature
- **Inputs:** None
- **Returns:** `int` — The next byte of data, or -1 if the end of the stream is reached.

### Example (Kotlin)
```kotlin
val byte = inputStream.read()
```
