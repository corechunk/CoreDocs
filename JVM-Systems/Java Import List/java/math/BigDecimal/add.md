# Method: add(BigDecimal augend)
## Class: `java.math.BigDecimal`

Returns a `BigDecimal` whose value is `(this + augend)`.

### Signature
- **Inputs:** `augend: BigDecimal` — Value to be added.
- **Returns:** `BigDecimal` — `this + augend`.

### Example (Kotlin)
```kotlin
val sum = price.add(tax)
```
