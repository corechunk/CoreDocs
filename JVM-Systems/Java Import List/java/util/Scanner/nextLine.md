# Method: nextLine()
## Class: `java.util.Scanner`

Advances this scanner past the current line and returns the input that was skipped.

### Signature
- **Inputs:** None
- **Returns:** `String` — The line that was skipped.

### Example (Kotlin)
```kotlin
val sc = Scanner(System.`in`)
val line = sc.nextLine()
```
