# Method: hasNext()
## Class: `java.util.Scanner`

Returns true if this scanner has another token in its input.

### Signature
- **Inputs:** None
- **Returns:** `boolean` — `true` if and only if this scanner has another token.

### Example (Kotlin)
```kotlin
while (sc.hasNext()) { ... }
```
