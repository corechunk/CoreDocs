# ⏳ Lifetimes and Environment Capture

## 1. Variable Lifetimes
Defines the precise duration during program execution that a memory allocation remains valid.

## 2. Closure Captures
When a function reference escapes its defining lexical scope, captured environment variables are moved from stack frames onto managed heap memory blocks to preserve state.
